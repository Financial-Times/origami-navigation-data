machine:
  node:
    version: '6'

test:
  override:
    - make verify
    - make test
    - make whitesource

deployment:
  release:
    branch: master
    commands:
      - make deploy-s3
      - make release-log
      - make update-cmdb
